# Hello, world

The smallest possible Inkdown program. Everything in this file is ordinary
Markdown — except the fenced code block below, which is the program:

```inkdown
print("Hello, world!")
```

Run it with:

```
inkdown run examples/hello.md
```
